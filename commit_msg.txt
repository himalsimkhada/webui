BIND tab polish round

- Add Zone modal: toggle .open (was .hidden) so the modal actually shows
  within the portal's .modal-mask CSS; after create, clear search/filter
  and open the new zone so it is never hidden by a stale filter
- Zones filter: replace native <select> with a custom dropdown that stays
  inside the Zones card (no spillover); persisted in JS state
- Add zone: raw file entry via the Raw Zone File textarea (type it, like
  Edit Raw File) instead of an upload button; zone name auto-fills from
  $ORIGIN; removed upload control
- Dig: when Server is left empty, run plain dig (no @default server) and
  show the command without @server
- Remove Host Mapper hint and zones-list placeholder hint; drop his
  personal domain from the mapper example
- Persist current view + BIND sub-tab in localStorage so a page refresh
  returns to the same tab instead of the dashboard